*.本单元提供对话框,进度条,提示信息,输入框,密码输入框等

&.链接库释放时,会主动调用释放过程.所以可以在其它地方初始化需要的对话框,但退出时不释放它.
例如:
在子窗体中PopMsg_Init初始化弹出式对话框,然后弹出提示信息并退出,提示信息可以保留.

&.Dlg_InputBox, Dlg_InputPWDBox中nSize是返回的nValue的最大值,且nSize = nValueSize - 1,因为末尾需添加一个\0来结尾.